jinja2>=3.0
markdown
mkdocs>=1.4.2
mkdocs-material
mkdocs-material-extensions
pymdown-extensions
pygments
mkdocs-mermaid2-plugin
mkdocs-video
mkdocs-git-revision-date-localized-plugin
markdown-captions
mkdocs-glightbox
# Pinned to 0.3.10: 0.3.11+ added a hard dependency on a package called
# "properdocs" that prints FUD urging a switch away from MkDocs and
# ships dormant sys.modules-hijacking code (unused unless imported, but
# still shipped) -- see three-world-launch-phases-ToDo.md #142.
mkdocs-section-index==0.3.10

# Same "properdocs" problem as above, same fix: pinned to 1.2.2, the last
# release before 1.2.3 added a hard dependency on it. Used to retire
# docs/fffx/PackingShapes.md in favor of FFFX's canonical copy while still
# redirecting Cabinet's old URL there -- see mkdocs.yml's `redirects` plugin
# config.
mkdocs-redirects==1.2.2




